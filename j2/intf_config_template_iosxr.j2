{%for i,j in interfaces.items()-%}
    interface {{i}}
    description {{j.intf_description}}
    ipv4 address {{j.ipv4_addr}}
    ipv6 address {{j.ipv6_addr}}
    ipv6 address {{j.ipv6_addr_ll}} link-local
    no shutdown
{%endfor-%}